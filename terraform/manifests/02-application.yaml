# Catalog Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-service
  namespace: retail-store
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog-service
  template:
    metadata:
      labels:
        app: catalog-service
    spec:
      containers:
      - name: catalog-service
        image: public.ecr.aws/docker/library/node:18-alpine
        command: ["sh", "-c", "echo 'Catalog service starting...'; sleep 3600"]
        env:
        - name: DB_HOST
          value: "mysql"
        - name: DB_PASSWORD
          value: "catalog_password"
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: retail-store
spec:
  selector:
    app: catalog-service
  ports:
  - port: 3000
    targetPort: 3000

# Orders Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-service
  namespace: retail-store
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orders-service
  template:
    metadata:
      labels:
        app: orders-service
    spec:
      containers:
      - name: orders-service
        image: public.ecr.aws/docker/library/node:18-alpine
        command: ["sh", "-c", "echo 'Orders service starting...'; sleep 3600"]
        env:
        - name: DB_HOST
          value: "postgresql"
        - name: DB_PASSWORD
          value: "orders_password"
        ports:
        - containerPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: retail-store
spec:
  selector:
    app: orders-service
  ports:
  - port: 3001
    targetPort: 3001

# UI Service (with LoadBalancer)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui-service
  namespace: retail-store
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ui-service
  template:
    metadata:
      labels:
        app: ui-service
    spec:
      containers:
      - name: ui-service
        image: nginx:alpine
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: ui-service
  namespace: retail-store
spec:
  type: LoadBalancer
  selector:
    app: ui-service
  ports:
  - port: 80
    targetPort: 80
