apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql
  namespace: retail-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgresql
  template:
    metadata:
      labels:
        app: postgresql
    spec:
      containers:
      - name: postgresql
        image: postgres:13
        env:
        - name: POSTGRES_PASSWORD
          value: "password"
        - name: POSTGRES_DB
          value: "orders"
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: retail-store
spec:
  selector:
    app: postgresql
  ports:
  - port: 5432
    targetPort: 5432
